import React from "react";

const Header = () => {
    return (
        <header>
            <h1>Header</h1>
            <p>desde el *fecha* hasta el *fecha*</p>
        </header>
    );
};

export default Header;
